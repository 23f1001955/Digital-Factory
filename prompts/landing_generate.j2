You are a frontend developer. Create a premium landing page HTML for a Gumroad digital product.

Product:
- Name: {{ display_name }}
- Niche: {{ niche }}
- Type: {{ product_type }}
- Gumroad URL: {{ gumroad_url }}
- CTA Button Text: {{ cta_text }}

Available images (use these URLs in the page):
{% for name, url in images.items() %}
- {{ name }}: {{ url }}
{% endfor %}

Requirements:
1. Single self-contained HTML file with embedded CSS
2. Responsive design (mobile-first)
3. Clean, modern, premium aesthetic with smooth animations
4. Hero section with the hero_banner image
5. Feature section showcasing key value props
6. Use benefit_visual and feature_showcase images
7. Prominent CTA button linking to {{ gumroad_url }} with text "{{ cta_text }}"
8. No external dependencies (no CDN links, no external fonts)
9. Dark or light theme - choose based on the product type
10. Smooth scroll and hover effects using CSS only (no JavaScript libraries)

Output ONLY the complete HTML. No explanations, no markdown formatting.
